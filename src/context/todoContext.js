// import { createContext, useContext } from "react";


// export const todoContextProvider = createContext(undefined)

// export const  useTodoContext = ()=>{
//     const todoInfo = useContext(todoContextProvider);
//     if(todoContextProvider===undefined){
//         throw new Error("enter the todoInfo in the value in provider")
//     }
//     return todoInfo;
// }